#!/usr/bin/env python3

# To make sure pylint is happy with the tests, run:
#   python3 setup.py develop
# every time we add or remove new top-level packages to the project.

from setuptools import setup


setup(name='PDP', packages=['pdp'])

